function isExpectedArrayLength(array, expectedLength) {
  return array.length === expectedLength;
}

export { isExpectedArrayLength };
// NOTE: 함수명 === 파일명을 따라야 하는지.. 파일명은 더 포괄적인 이름으로 해 확장성을 고려하는게 좋은지 궁금합니다.
