const CONFIG = Object.freeze({
  MIN: Object.freeze({
    LOTTO_NUMBER: 1,
    RANK_COUNT: 3,
  }),
  MAX: Object.freeze({
    LOTTO_NUMBER: 45,
    LOTTO_LENGTH: 6,
  }),
  LOTTO_PRICE: 1000,
  INITIAL_NUMBER: 0,
});

export default CONFIG;
