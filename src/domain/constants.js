export const LOTTO_RULE = Object.freeze({
  MIN_RANGE: 1,
  MAX_RANGE: 45,
  LENGTH: 6,
  PRICE: 1000,
  MAX_BUY_MONEY: 100000,
});

export const RANK_RULE = Object.freeze({
  MIN_COUNT: 3,
  SECOND_PRIZE_MATCH_COUNT: 5,
});

export const INITIAL_NUMBER = 0;
export const ONE_TICKET = 1;

export const ANSWER = Object.freeze({
  RE_START: 'y',
  END: 'n',
});
