import { MESSAGES } from "../constant/messages";
import Console from "../utils/console"
const { readLineAsync, print } = Console;

const inputBuyMoney(){
     readLineAsync(MESSAGES.prefix+MESSAGES.inputBuyMoney);
    
}