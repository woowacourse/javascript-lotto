## 기능 요구 사항

- 초기 상태: 구입 input
- 구입 버튼 클릭 시 구입한 로또 정보와 당첨번호/보너스번호 input 렌더링
    - 로또 개수
    - 생성된 로또 정보
- 결과 확인 버튼 클릭 시 당첨 통계 모달 렌더링
    - 당첨 개수
    - 수익률
- 다시 시작 버튼 클릭 시 초기 상태로
- 모달 닫기 버튼 클릭 시 모달만 닫힘

우선 로직 완성 -> 퍼블리싱

## 리팩토링

1. 객체 리터럴 -> 클래스 변경 (기존 돔 요소를 참조해야 하는 상황이 빈번함)
    - 어디까지가 constructor / 어디부터가 render 책임인지 기준이 필요함
2. 과도한 접미사 제거
6. id/class 상수화
8. 트리거 추가/init 등 반복되는 패턴 모듈화 가능한지 검토
8. 폰트시스템 클래스 할당도 제거?? 그냥 클래스 없이 css변수만 가져다 쓰는 게 나을 것 같기도 (스타일링에 대해 js와 css 두곳을 확인해야 함)

## 수정 및 기능 추가

1. input type number -> 마우스휠 제한
2. 당첨 번호 입력 시 키보드 조작(화살표로 다음 넘어가게)
3. 전체 예외 잡아서 alert 또는 각 입력의 errorMessage로

## 추가

6. winningNumbers 입력 시 input 6개를 사용하는 방식에 대해 더 고민
7. 분리 가능한 컴포넌트 분리 -> 스타일링을 컴포넌트에 따라 설정할지 클래스로 할당할지 결정
13. input label? 암튼 접근성
15. 필드셋 사용 검토

## 프로그래밍 요구 사항

### 모듈화에 대해 고민한다. - 도메인과 UI 관심사의 분리

- 1단계에서 구현한 도메인 로직을 (최대한) 수정하지 않고, UI만 변경한다.

### 일관성 있고 의도가 드러나는 마크업을 작성하기 위해 노력한다.

- 목적에 맞는 HTML 태그를 사용한다.
- CSS 속성 선언 순서의 일관성을 고려한다.

### CSS 문법 사용에 익숙해진다.

- CSS 속성은 가능하면 축약형(shorthand)을 사용한다.
- flexbox를 활용해 레이아웃을 구성한다.
